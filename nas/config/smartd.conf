# Monitor all detected drives, avoid waking standby disks, and run one short self-test monthly.
# SnapRAID scrub provides the recurring protected-data read and parity verification.
DEVICESCAN -a -o on -S on -n standby,q -s S/../01/./02 -m <nomailer> -M exec /usr/local/sbin/nas-notify
