# This NAS deliberately uses IPv4 only. The LAN's advertised IPv6 path is
# unreliable and caused indefinite Arch News feed connection attempts.
net.ipv6.conf.all.disable_ipv6 = 1
net.ipv6.conf.default.disable_ipv6 = 1
