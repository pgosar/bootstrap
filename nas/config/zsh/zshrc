# Generated by NAS bootstrap.
# /etc/zsh/zprofile sources /etc/profile for login shells; this covers
# non-login interactive zsh shells while the reminder script prevents duplicates.
if [ -r /etc/profile.d/nas-kernel-reminder.sh ]; then
  emulate sh -c 'source /etc/profile.d/nas-kernel-reminder.sh'
fi
