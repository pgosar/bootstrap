[Unit]
Description=Create a cross-disk Docker state backup
RequiresMountsFor=/mnt/disk1 /mnt/disk2 /mnt/disk3 /mnt/parity
After=local-fs.target

[Service]
Type=oneshot
ExecStart=/usr/local/sbin/nas-docker-state-backup
